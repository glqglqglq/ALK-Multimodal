[project]
name = "m2m-alk"
version = "0.1.0"
description = "M2M-ALK multimodal CT and WSI feature fusion for ALK testing triage"
requires-python = ">=3.10"
readme = "README.md"
license = { text = "LicenseRef-TODO" }
authors = [{ name = "M2M-ALK authors" }]

[tool.setuptools]
packages = ["m2m_alk"]
